# Métodos HTTP adequados

Use o método HTTP adequado de acordo com a operação: `GET (read)`, `POST (create)`, `PUT/PATCH (replace/update)` e `DELETE (to delete a record)` e responda com `405 Method Not Allowed` se o método solicitado não for apropriado para o recurso solicitado.
